{:kotoba.lang.q9.migration-record/version 1
 :repository "orgs/kotoba-lang/com-aftership"
 :as-of "2026-09-10"
 :wave :wave-1
 :disposition :partial-and-running
 :record-author "whole-component port, GENERATED by kotoba-lang scripts/gen-whole-component.cljs"
 :generated-not-hand-ported
 "Every entity name, plural, id prefix, field list, required list, coercion
  pair and ref pair is derived from src/aftership/main.cljc's own entity-specs
  table. The generator exists because the third port in this cohort was hand
  written and carried the previous repository's ref field name into the expand
  oracle, where it would have folded over nothing and gone green."
 :reserved-name-collision
 {:collides true :name "query" :cleared-by :rename-in-the-migrated-component
  :detail "main.cljc declares a public `query`, reserved as a head 2026-09-06. The migrated component names it store-query; the .cljc is unchanged."}
 :shape {:refs "refs=none -- expand folds over nothing in this repository" :ref-count 0 :expand-fold :not-exercised}
 :builds {:amu-check {:status :ok :exit 0}
          :amu-compile-native {:status :ok :exit 0 :target :aarch64-macos}}
 :executed
 {:harness "cc -O2 amu/tools/kexe_loader.c, then KEXE_FUEL=100000000 ./kexe-loader <sym>.bin <offset> <arity> aarch64 7,23 [args]"
  :main {:value 25 :meaning "route-count: five routes per entity"}
  :oracle-expand
  {:values {0 546326893 1 546326893 2 591510699 3 546326893 4 546326893 5 546326893}
   :reading
   "$READING"}}
 :not-claimed
 ["No consumer cutover. main.cljc remains the oracle."
  "No value-by-value parity against the CLJC oracle."
  "wasm32 was not built; only aarch64-macos was compiled and executed."]

 :parity
 {:harness "q9-oracle.cljs, GENERATED by kotoba-lang scripts/gen-parity-harness.cljs"
  :command "nbb --classpath src:<kotoba-lang/text>/src q9-oracle.cljs"
  :measured "2026-09-11, against the component compiled to aarch64-macos (234270 bytes) and executed through tools/kexe_loader.c"
  :agrees {:route-count "25 on both sides" :as-bool "9 of 9" :page-limit "8 of 8"}
  :route-count-note
  "25, not 30: five entities rather than six. The count is derived from the
   entity table on BOTH sides, so the 30s elsewhere in the cohort are
   evidence about a derivation rather than a constant that matches."
  :how-much-each-layer-carries
  "Not equally. route-count VARIES across the cohort (25 here or there, 30
   elsewhere), so its agreement is evidence. page-limit cannot vary: every one
   of the twenty pilots declares default-limit 20 and max-limit 100, so 8 of 8
   is uniform by construction and is the weakest row here. as-int is the only
   row that ever differs."
  :disagrees
  {:as-int-trailing-garbage
   {:input "12abc" :artifact 0 :oracle-under-nbb 12
    :reading "The cohort-wide reader-conditional divergence recorded in kotoba-lang/lang/q9-wave1-tranche-1.edn. Not a defect in this port: the oracle's own :clj and :cljs branches disagree and the component matches :clj."}}
  :not-covered
  "The store, filter, pagination, handler and expand layers, which need fixtures
   shaped to this repository's own entities."}

 :harness-relevelled-2026-09-11
 {:added "the entity-table layer"
  :why
  "This harness was generated before that layer existed and compared only the
   route count, as-int, as-bool and page-limit. Of those, page-limit CANNOT vary
   across this cohort -- every pilot declares default-limit 20 and max-limit 100
   -- so the harness carried one row of per-repository signal and three that
   could not distinguish this repository from any other."
  :measured {:entity-table "5 of 5 agreeing, against the component compiled to aarch64-macos and executed"}
  :note
  "The row count follows the entity table on both sides: 5 entities here."}
}
